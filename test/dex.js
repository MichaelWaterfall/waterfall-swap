/*const Dex = artifacts.require('Dex');

contract('Dex', () => {
    it('should deploy contract', async () => {
        const dex = await Dex.deployed();
        assert(dex.address != '');
    });
});*/